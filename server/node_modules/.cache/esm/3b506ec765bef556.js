let mongoose;_65b‍.x([["Races",()=>Races]]);_65b‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema, model } = mongoose

const racesSchema = new Schema({
  track: String,
  country: String,
  city: String,
  date: Number,
  winner: String,
  fastestLap: String,
  polePosition: String,
})

       const Races = model('races', racesSchema)
