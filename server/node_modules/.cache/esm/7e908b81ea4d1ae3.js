let Driver,Races;_ea4‍.x([["resolvers",()=>resolvers]]);_ea4‍.w("../models/drivers",[["Driver",["Driver"],function(v){Driver=v}]]);_ea4‍.w("../models/races",[["Races",["Races"],function(v){Races=v}]]);


       const resolvers = {
  Query: {
    drivers: () => Driver.find(),
    races: () => Races.find(),
  },
  Mutation: {
    addDriver: async (_, { name, age, country, team }) => {
      const driver = new Driver({ name, age, country, team })
      await driver.save()
      return driver
    },
    addRace: async (_, { name }) => {
      const race = new Races({ name })
      await race.save()
      return race
    },
  },
}
