let mongoose;_df9‍.x([["Driver",()=>Driver]]);_df9‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema, model } = mongoose

const driverSchema = new Schema({
  name: String,
  age: Number,
  country: String,
  team: String,
  racesWon: Array,
})

       const Driver = model('driver', driverSchema)
